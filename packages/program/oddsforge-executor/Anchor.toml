[toolchain]
package_manager = "yarn"
solana_version = "4.0.0"

[features]
resolution = true
skip-lint = false

[programs.devnet]
oddsforge_executor = "3XVn1g5q5NbGB7r3u8dYfWsmZw1n3bqCFXjiawvDSBBe"

[registry]
url = "https://api.apr.dev"

[provider]
cluster = "localnet"
wallet = "~/.config/solana/id.json"

[scripts]
test = "yarn run ts-mocha -p ./tsconfig.json -t 1000000 tests/**/*.ts"
